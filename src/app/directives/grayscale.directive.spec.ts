import { GrayscaleDirective } from './grayscale.directive';

describe('GrayscaleDirective', () => {
  it('should create an instance', () => {
    const directive = new GrayscaleDirective();
    expect(directive).toBeTruthy();
  });
});
